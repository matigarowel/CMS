import { CustomLayoutDirective } from './custom-layout.directive';

describe('CustomLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
