import { CustomLayoutGapDirective } from './custom-layout-gap.directive';

describe('CustomLayoutGapDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLayoutGapDirective();
    expect(directive).toBeTruthy();
  });
});
