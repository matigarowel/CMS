import { CustomFlexDirective } from './custom-flex.directive';

describe('CustomFlexDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFlexDirective();
    expect(directive).toBeTruthy();
  });
});
