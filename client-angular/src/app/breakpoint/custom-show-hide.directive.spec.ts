import { CustomShowHideDirective } from './custom-show-hide.directive';

describe('CustomShowHideDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomShowHideDirective();
    expect(directive).toBeTruthy();
  });
});
